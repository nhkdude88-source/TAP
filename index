import React, { useState } from 'react';
import { ShieldAlert, HeartHandshake, Activity, BrainCircuit, ChevronDown, ChevronUp, Eye, Hand, MessageSquareQuote } from 'lucide-react';

const pouData = [
  {
    id: 1,
    name: "POU TUARONGO",
    brainRegion: "Brain Stem / Survival Brain",
    color: "bg-red-900",
    lightColor: "bg-red-50",
    textColor: "text-red-900",
    borderColor: "border-red-200",
    icon: ShieldAlert,
    stages: [
      "1. Te Waharoa & Waerea",
      "2. Whakawhanaungatanga & Wero"
    ],
    indicators: "Yelling, posturing, running, refusing, 'defiant' behaviour. Nervous system is in fight/flight.",
    action: "Ground yourself (He Urungā Tū). Do not pick up the weapon (argue). Be silent. Hold the space safely until the 'earthquake' stops.",
    script: "His Survival Brain (Pou Tuarongo) is activated. This is a safety check (Wero), not defiance. Lower demands and hold the space until his nervous system settles."
  },
  {
    id: 2,
    name: "POU TOKOMANAWA",
    brainRegion: "Limbic System / Emotional Brain",
    color: "bg-green-800",
    lightColor: "bg-green-50",
    textColor: "text-green-800",
    borderColor: "border-green-200",
    icon: HeartHandshake,
    stages: [
      "3. Kohikohi & Karanga",
      "4. Āta Whakaaro & Whaikōrero"
    ],
    indicators: "Pausing, looking around, checking for safety, glaring but quiet. Assessing friend or foe.",
    action: "Soften your eyes (Kanohi Kitea). Use their name. Acknowledge identity (Whakapapa) to establish relational safety before discussing the problem.",
    script: "His Emotional Brain (Pou Tokomanawa) is scanning for threats. He needs a Karanga. Soften your tone, use his name, and acknowledge him before talking about schoolwork."
  },
  {
    id: 3,
    name: "POU TĀHŪ",
    brainRegion: "Diencephalon & Cerebellum / Movement Brain",
    color: "bg-blue-800",
    lightColor: "bg-blue-50",
    textColor: "text-blue-800",
    borderColor: "border-blue-200",
    icon: Activity,
    stages: [
      "5. Tātai & Waiata",
      "6. Whakamahi & Koha"
    ],
    indicators: "Breathing slower, waiting, but fidgeting or unable to sit perfectly still. Needing an outlet.",
    action: "Stand side-by-side. Engage in a shared, predictable, rhythmic activity (walking, deep breathing, a simple task) to co-regulate.",
    script: "Before we access the Thinking Brain for math, we must regulate the Movement Brain (Pou Tāhū). Let's use a rhythmic activity—a quick walk or predictable task—to settle his body first."
  },
  {
    id: 4,
    name: "POU KAIĀWHĀ",
    brainRegion: "Cerebral Cortex / Thinking Brain",
    color: "bg-amber-600",
    lightColor: "bg-amber-50",
    textColor: "text-amber-800",
    borderColor: "border-amber-200",
    icon: BrainCircuit,
    stages: [
      "7. Whai Whakaaro & Hongi",
      "8. Mana Motuhake & Hākari"
    ],
    indicators: "Relaxed body language, making eye contact, focused, smiling or ready to engage.",
    action: "Celebrate mastery. Engage in high-level reflection. Discuss the repair/plan using cognitive logic.",
    script: "The environment is Noa (Safe). His Thinking Brain (Pou Kaiāwhā) is online. Now we can do the cognitive work, reflect on the behaviour, and engage in learning."
  }
];

export default function App() {
  const [expandedId, setExpandedId] = useState(null);

  const toggleExpand = (id) => {
    setExpandedId(expandedId === id ? null : id);
  };

  return (
    <div className="min-h-screen bg-gray-100 flex justify-center font-sans pb-10">
      <div className="w-full max-w-md bg-gray-100 sm:shadow-lg sm:my-6 sm:rounded-2xl overflow-hidden flex flex-col">
        
        {/* Header Section */}
        <header className="bg-slate-900 text-white p-6 pb-8 rounded-b-3xl shadow-md z-10">
          <h1 className="text-2xl font-bold tracking-tight mb-1">Te Ara Pōwhiri</h1>
          <h2 className="text-sm text-slate-300 font-medium tracking-wide uppercase">Pouwhirinaki Field Guide</h2>
          <p className="mt-4 text-sm text-slate-400 leading-relaxed">
            Select the brain state you are currently observing to access your casework strategy and Kaiako coaching scripts.
          </p>
        </header>

        {/* Main Content - Accordion List */}
        <main className="flex-1 px-4 -mt-4 relative z-0">
          <div className="space-y-4">
            {pouData.map((pou) => {
              const isExpanded = expandedId === pou.id;
              const Icon = pou.icon;

              return (
                <div 
                  key={pou.id} 
                  className={`bg-white rounded-xl shadow-sm border overflow-hidden transition-all duration-300 ease-in-out ${isExpanded ? 'ring-2 ring-slate-400 shadow-md' : 'border-gray-200'}`}
                >
                  {/* Accordion Header */}
                  <button
                    onClick={() => toggleExpand(pou.id)}
                    className={`w-full flex items-center justify-between p-4 text-left transition-colors duration-200 ${isExpanded ? pou.color : 'bg-white hover:bg-gray-50'}`}
                  >
                    <div className="flex items-center space-x-4">
                      <div className={`p-2 rounded-lg ${isExpanded ? 'bg-white/20 text-white' : pou.textColor + ' bg-gray-100'}`}>
                        <Icon size={24} strokeWidth={2} />
                      </div>
                      <div>
                        <h3 className={`font-bold text-lg ${isExpanded ? 'text-white' : 'text-gray-900'}`}>
                          {pou.name}
                        </h3>
                        <p className={`text-xs font-medium ${isExpanded ? 'text-white/80' : 'text-gray-500'}`}>
                          {pou.brainRegion}
                        </p>
                      </div>
                    </div>
                    <div>
                      {isExpanded ? (
                        <ChevronUp className="text-white" size={20} />
                      ) : (
                        <ChevronDown className="text-gray-400" size={20} />
                      )}
                    </div>
                  </button>

                  {/* Accordion Body */}
                  <div 
                    className={`transition-all duration-300 ease-in-out overflow-hidden ${isExpanded ? 'max-h-[1000px] opacity-100' : 'max-h-0 opacity-0'}`}
                  >
                    <div className="p-5 space-y-6">
                      
                      {/* Phases Alignment */}
                      <div className={`p-3 rounded-lg border ${pou.lightColor} ${pou.borderColor}`}>
                        <h4 className={`text-xs uppercase font-bold mb-2 tracking-wider ${pou.textColor}`}>Alignment Matrix</h4>
                        <ul className="text-sm text-gray-700 space-y-1 font-medium">
                          {pou.stages.map((stage, index) => (
                            <li key={index} className="flex items-center">
                              <span className="w-1.5 h-1.5 rounded-full bg-gray-400 mr-2"></span>
                              {stage}
                            </li>
                          ))}
                        </ul>
                      </div>

                      {/* Indicators */}
                      <div>
                        <div className="flex items-center space-x-2 mb-1.5">
                          <Eye size={18} className="text-slate-500" />
                          <h4 className="font-bold text-slate-800 text-sm uppercase">What You See</h4>
                        </div>
                        <p className="text-sm text-slate-600 leading-relaxed pl-6 border-l-2 border-slate-200">
                          {pou.indicators}
                        </p>
                      </div>

                      {/* Action */}
                      <div>
                        <div className="flex items-center space-x-2 mb-1.5">
                          <Hand size={18} className="text-slate-500" />
                          <h4 className="font-bold text-slate-800 text-sm uppercase">Your RTLB Action</h4>
                        </div>
                        <p className="text-sm text-slate-600 leading-relaxed pl-6 border-l-2 border-slate-200">
                          {pou.action}
                        </p>
                      </div>

                      {/* Script */}
                      <div className={`mt-6 p-4 rounded-xl border-l-4 shadow-sm bg-white border ${pou.borderColor}`} style={{ borderLeftColor: pou.color }}>
                        <div className="flex items-center justify-between mb-2">
                          <div className="flex items-center space-x-2">
                            <MessageSquareQuote size={18} className={pou.textColor} />
                            <h4 className={`font-bold text-sm uppercase ${pou.textColor}`}>Kaiako Coaching Script</h4>
                          </div>
                        </div>
                        <p className="text-sm text-slate-700 italic font-medium leading-relaxed">
                          "{pou.script}"
                        </p>
                      </div>

                    </div>
                  </div>
                </div>
              );
            })}
          </div>
        </main>
        
        {/* Footer */}
        <footer className="mt-8 py-6 text-center text-xs text-gray-400">
          <p>Created for Cluster 21 Pouwhirinaki Practice.</p>
          <p className="mt-1">Daelan Karangaroa PhD Research © 2026</p>
        </footer>
      </div>
    </div>
  );
}
